def convert(arr):
    return [str(arr[i]) for i in range(len(arr))]

def convert(arr):
	newArr = []
	for i in arr:
		newArr.append(str(i))

"""
learned:
one line for loops 
are interesting 
you define the var and the 
change before the loop
"""